/**
 * Marketing layout — wraps landing, about, pricing pages.
 * No auth required. Shares locale routing from parent [locale]/layout.tsx.
 */
export default function MarketingLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
